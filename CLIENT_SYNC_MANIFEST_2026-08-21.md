# BeiDou Client Sync Manifest

日期：2026-08-21  
仓库：`noobytyty/BeiDou-Server`  
用途：记录今天装备词缀系统变更中需要客户端同步、无需客户端同步以及后续客户端开发的内容。

## 1. 同步结论

- 今天的装备词缀核心变更主要在服务端 Java、Flyway 和服务端 i18n。
- 当前 v83 客户端没有装备实例动态名称/Tooltip 扩展能力，因此**本次不需要直接复制 Java、SQL 或 i18n 文件到客户端**。
- 拾取提示、`@inspect`、`@affix` 使用服务端聊天文本展示词缀名称、T 阶和实际贡献值。
- 如果未来要求原版装备 Tooltip 直接显示动态词缀，必须修改客户端程序、Tooltip 渲染和相关封包，单独制作客户端版本。

## 2. 今天服务端已完成、客户端无需同步

### 2.1 装备词缀配置和运行时

- T1–T12 等级词缀池；
- 品质决定词缀数量，需求等级决定词缀阶级窗口；
- 主/副词缀池和部位权重；
- Overall 额外主词缀；
- 耳环独立经济词缀池；
- 普通短杖/法杖随机元素专精；
- 元素法杖/魔杖固定对应元素；
- 同一装备最多一个元素词缀；
- 元素技能伤害加成和 25% 元素上限；
- 混合词缀实际属性贡献和辅助属性折算；
- 低价值混合词缀降权。

### 2.2 服务端展示资源

服务端已经写入中文和英文 T1–T12 词缀命名键：

```text
gms-server/src/main/resources/i18n/message_zh_CN.properties
gms-server/src/main/resources/i18n/message_en_US.properties
```

客户端不读取这些 `.properties` 文件，也不要将它们转换成客户端 `String.wz`。
这些名称由服务端格式化后通过聊天封包发送。

### 2.3 数据库迁移

以下迁移由服务端 Flyway 执行，客户端不需要同步：

```text
V1.11.24–V1.11.52
```

包括等级池、混合词缀、元素词缀、部位权重、百分比上限相关配置和低价值混合词缀降权。

## 3. 客户端可选同步：装备静态文本

如果客户端装备静态名称或说明尚未包含今天涉及的装备，应单独检查现有
`String/Eqp.img`。本次词缀系统本身不会新增装备 Item ID，也不会修改装备
图标、外观或静态装备属性，因此默认不生成 WZ/IMG 补丁。

检查范围：

```text
Data/String/Eqp.img
EN/String/Eqp.img
Data/Character/
```

只有发现确实新增了装备节点或静态文本缺失时，才制作对应补丁；不要为了服务端动态词缀名称向 `Eqp.img` 添加伪造动态节点。

## 4. 客户端未来开发项：动态 Tooltip

原版 v83 客户端不会根据装备实例读取服务端动态词缀并生成名称。若要实现客户端 Tooltip，需要客户端项目配合：

1. 扩展装备实例数据或新增词缀封包；
2. 客户端保存词缀代码、T 阶、原始值和品质；
3. Tooltip 根据客户端语言显示独立 T1–T12 名称；
4. 混合词缀显示实际拆分贡献，而不是只显示共享原始值；
5. 处理交易、商店、装备比较和重登后的实例数据。

在客户端改造完成前，推荐交互流程保持：

```text
拾取装备 -> 聊天框显示词缀摘要
@inspect  -> 列出装备栏位和词缀数量
@inspect 1 或 @affix 1 -> 显示完整词缀、名称、T 阶和实际贡献
```

## 5. 服务端验证

本次服务端验证已执行：

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH="$JAVA_HOME/bin:$PATH"
mvn -pl gms-server -DskipTests compile
git diff --check
```

编译和差异检查通过。Flyway 迁移仍需在真实 MySQL 环境验证升级顺序和索引状态。

## 6. 客户端验收结论

本次不需要生成 `.img` 补丁。客户端验收重点是：

- 聊天框能正常显示服务端发送的中文/英文词缀文本；
- 长文本换行和特殊字符不会造成客户端断包或乱码；
- `@inspect` 输出不会超过聊天消息长度限制；
- 中英文客户端分别连接对应服务端语言配置后，词缀名称显示正确。

